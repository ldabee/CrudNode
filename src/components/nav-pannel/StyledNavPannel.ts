import styled from 'styled-components';

export const Pannel = styled.div({
    minHeight: "100vh",
    height: "710px",
    overflowY: "auto",
    overflowX: "hidden",
    padding: "15px",
    backgroundColor: "rgb(143, 153, 176)",
})

